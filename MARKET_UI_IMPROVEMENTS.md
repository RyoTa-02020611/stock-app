# マーケット画面 UI/UX 改善ガイド

## 📋 目次
1. [新しい構成図](#新しい構成図)
2. [実装コンポーネント](#実装コンポーネント)
3. [実装手順](#実装手順)
4. [モックデータ](#モックデータ)
5. [拡張案](#拡張案)

---

## 新しい構成図

```
MarketPage (app/market/page.tsx)
├── MarketHeader (タイトル・説明)
├── MarketFilters (市場フィルタ)
├── SortButtons (並び替えボタン - 強調表示対応)
│   ├── SortButton (個別ボタン)
│   │   ├── activeSort state で管理
│   │   ├── アクティブ時: 色変更 + 下線 + ↑↓アイコン
│   │   └── 非アクティブ時: 通常スタイル
├── SearchBar (検索バー)
├── StockTable (銘柄テーブル)
│   ├── StockTableHeader (テーブルヘッダー)
│   ├── StockTableRow (銘柄行)
│   │   ├── StockSparkline (ミニチャート)
│   │   │   └── Recharts LineChart (120px × 40px)
│   │   ├── StockInfo (銘柄情報)
│   │   └── onClick → 詳細表示
│   └── StockTableFooter (件数表示)
└── StockDetailModal (銘柄詳細モーダル)
    ├── StockDetailHeader (銘柄名・価格)
    ├── StockDetailChart (1D〜1Yチャート)
    ├── StockDetailMetrics (基本指標)
    └── StockDetailNews (関連ニュース)
```

---

## 実装コンポーネント

### 1. SortButtons（並び替えボタン - 強調表示）

**ファイル**: `app/components/market/SortButtons.tsx`

**機能**:
- アクティブなソート条件を強調表示
- ボタンの色変更、下線、↑↓アイコン

**State管理**:
```typescript
const [activeSort, setActiveSort] = useState<{
  type: SortType
  direction: 'asc' | 'desc'
}>({ type: 'gainers', direction: 'desc' })
```

### 2. StockSparkline（ミニチャート）

**ファイル**: `app/components/market/StockSparkline.tsx`

**機能**:
- 5日または1ヶ月の価格推移を表示
- Recharts LineChart を使用
- サイズ: 120px × 40px

### 3. StockDetailModal（銘柄詳細モーダル）

**ファイル**: `app/components/market/StockDetailModal.tsx`

**機能**:
- 行クリックでモーダル表示
- 現在価格、チャート、基本指標、ニュースを表示

---

## 実装手順

### ステップ1: SortButtons コンポーネントの作成

### ステップ2: StockSparkline コンポーネントの作成

### ステップ3: StockDetailModal コンポーネントの作成

### ステップ4: MarketPage の更新

---

## モックデータ

### 価格履歴データ（Sparkline用）

```typescript
interface PriceData {
  date: string
  price: number
}

// 5日間の価格データを生成
function generateSparklineData(basePrice: number, days: number = 5): PriceData[] {
  const data: PriceData[] = []
  const today = new Date()
  
  for (let i = days - 1; i >= 0; i--) {
    const date = new Date(today)
    date.setDate(date.getDate() - i)
    
    // ランダムな変動（±5%）
    const variation = (Math.random() - 0.5) * 0.1
    const price = basePrice * (1 + variation)
    
    data.push({
      date: date.toISOString().split('T')[0],
      price: Number(price.toFixed(2)),
    })
  }
  
  return data
}
```

---

## 拡張案

1. **ソート方向の切り替え**: クリックで昇順/降順を切り替え
2. **複数ソート**: 複数の条件でソート
3. **チャート期間の切り替え**: 5日/1ヶ月/3ヶ月を切り替え
4. **詳細パネルのアニメーション**: スライドイン/フェードイン

---

詳細は実装コードを参照してください。

